@*@model CRM1._2.Models.Employee

<h2>Delete Employee</h2>
@using (Html.BeginForm("delete", "App", FormMethod.Post, new { id = "popupForm" }))
{
    @Html.HiddenFor(a=>a.EmployeeID)
    <div class="form-group">
        <label>First Name</label>
        <p>@Model.FirstName</p>
    </div>

    <div class="form-group">
        <label>Last Name</label>
        <p>@Model.LastName</p>
    </div>

    <div class="form-group">
        <label>Email</label>
        <p>@Model.Email</p>
    </div>

    <div class="form-group">
        <label>Address</label>
        <p>@Model.Address</p>
    </div>

    <div class="form-group">
        <label>Phone</label>
        <p>@Model.Phone</p>
    </div>

    <div>
        <input type="submit" value="Delete" />
    </div>
}*@